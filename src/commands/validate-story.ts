import chalk from "chalk";

export async function validateStoryCommand(storyFile: string) {
  console.log(chalk.yellow("Story validation not yet implemented"));
  console.log("Story file:", storyFile);
}
